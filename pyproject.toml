[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wc_cuda"
version = "0.1.0"
dependencies = []
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "wc_cuda._wc_cuda"
