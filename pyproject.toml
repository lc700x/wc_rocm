[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wc_xpu"
version = "0.1.3.post1"
dynamic = []
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
description = "Screen capture to PyTorch XPU tensors on Intel GPUs"
authors = [
    {name = "lc700x"},
]

[tool.maturin]
python-source = "python"
module-name = "wc_xpu._wc_xpu"
