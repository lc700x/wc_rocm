[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wc_rocm"
dynamic = ["version"]
dependencies = []
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "wc_rocm._wc_rocm"
