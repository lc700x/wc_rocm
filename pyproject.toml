[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wc_xpu"
dynamic = ["version"]
dependencies = []
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "wc_xpu._wc_xpu"
